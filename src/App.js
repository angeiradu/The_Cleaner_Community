import { BrowserRouter, Route, Routes } from "react-router-dom";

function App() {
  <BrowserRouter>
    <Routes>
      <Route element={<PageLayout />}>
    </Routes>
  </BrowserRouter> 
  );
}

export default App;
